// ignore_for_file: constant_identifier_names

enum ViewState {
  idle,
  busy,
}

enum AppMode {
  debug,
  uat,
  fakeService,
  release,
}

enum DialogType {
  success,
  info,
  warning,
  error,
}

enum RequestType {
  GET,
  POST,
  PUT,
  DELETE
}
